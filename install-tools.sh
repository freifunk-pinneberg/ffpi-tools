#!/bin/sh

install -v fastd-status.py /usr/local/bin
install -v ffgate-check /usr/local/bin
install -v alfred-announce.py /usr/local/bin
install -v vnstati.sh /usr/local/bin

install -v init.d/fastd /etc/init.d
